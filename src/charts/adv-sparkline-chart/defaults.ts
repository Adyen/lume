import { Options } from '@/mixins/options';

export const options: Options = {
  margins: {
    top: 0,
    right: 0,
    bottom: 0,
    left: 0,
  },
  showAxes: false,
  showArea: true,
  startOnZero: true,
};
